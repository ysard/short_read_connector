--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -36,13 +36,8 @@
 # THIRD PARTIES
 ################################################################################
 
-# We don't want to install some GATB-CORE artifacts
-SET (GATB_CORE_EXCLUDE_TOOLS     1)
-SET (GATB_CORE_EXCLUDE_TESTS     1)
-SET (GATB_CORE_EXCLUDE_EXAMPLES  1)
-
 # GATB CORE
-include (GatbCore)
+find_package(gatbcore 1.3.0)
 
 ################################################################################
 #  TOOLS GENERATION
--- a/tools/SRC_counter/CMakeLists.txt
+++ b/tools/SRC_counter/CMakeLists.txt
@@ -5,12 +5,12 @@
 ################################################################################
 
 # we get compilation definitions from the gatb-core part
-add_definitions (${gatb-core-flags})
+add_definitions (${GATBCORE_DEFINITIONS})
 
 # we give the headers directories from : 
 #       - from project source 
 #       - from GATB-CORE source
-include_directories (src  ${gatb-core-includes} ../common)
+include_directories (src  ${GATBCORE_INCLUDE_DIRS} ../common)
 
 # we define the files to be compiled
 file (GLOB_RECURSE  ProjectFiles  src/* ../common)
@@ -22,7 +22,7 @@
 
 
 # we define which libraries to be linked with project binary
-target_link_libraries (${PROJECT_NAME}  ${gatb-core-libraries})
+target_link_libraries (${PROJECT_NAME}  ${GATBCORE_LIBRARIES} pthread)
 
 
 # We copy the project binary to the 'bin' directory
--- a/tools/SRC_linker/CMakeLists.txt
+++ b/tools/SRC_linker/CMakeLists.txt
@@ -5,12 +5,12 @@
 ################################################################################
 
 # we get compilation definitions from the gatb-core part
-add_definitions (${gatb-core-flags})
+add_definitions (${GATBCORE_DEFINITIONS})
 
 # we give the headers directories from : 
 #       - from project source 
 #       - from GATB-CORE source
-include_directories (src  ${gatb-core-includes} ../common)
+include_directories (src  ${GATBCORE_INCLUDE_DIRS} ../common)
 
 # we define the files to be compiled
 file (GLOB_RECURSE  ProjectFiles  src/* ../common)
@@ -24,7 +24,7 @@
 
 
 # we define which libraries to be linked with project binary
-target_link_libraries (${PROJECT_NAME}  ${gatb-core-libraries})
+target_link_libraries (${PROJECT_NAME}  ${GATBCORE_LIBRARIES} pthread)
 
 
 # We copy the project binary to the 'bin' directory
--- a/thirdparty/quasi_dictionary/CMakeLists.txt
+++ b/thirdparty/quasi_dictionary/CMakeLists.txt
@@ -30,5 +30,5 @@
 # add_executable        (${PROJECT_NAME}  ${ProjectFiles})
 
 # we define which libraries to be linked with project binary
-target_link_libraries (${PROJECT_NAME})
+# target_link_libraries (${PROJECT_NAME})
 
